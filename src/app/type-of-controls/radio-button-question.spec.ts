import { RadioButtonQuestion } from './radio-button-question';

describe('RadioButtonQuestion', () => {
  it('should create an instance', () => {
    expect(new RadioButtonQuestion()).toBeTruthy();
  });
});
